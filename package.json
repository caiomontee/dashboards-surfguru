{
  "name": "dashboards-surfguru",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo: dashboard SurfGuru (frontend Vite + backend serverless na Vercel).",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.2",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.4.5"
  }
}
